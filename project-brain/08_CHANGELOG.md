# 08 — Changelog

> Chronological project history reconstructed from `git log` on branch `work/cambios-personales` (154 commits, first commit 2025-09-07, latest 2026-07-03). This is the factual record referenced by [05_ROADMAP.md](05_ROADMAP.md)'s "Completed" section and by [ADR-014](04_DECISIONS.md) (which explains why "M0–M4" is *not* a historical label).

---

| Date | Commit | Phase / Milestone | Summary |
|---|---|---|---|
| 2025-09-07/09 | `01edef5`, `dd91147` | Bootstrap | Initial repository; first functional `localStorage`-based version. |
| 2025-11-24 | `6ec0ab1` | **FASE 1** | "Estructura base y dashboards implementados" — base structure, admin/employee/client dashboards. |
| 2025-11-24→28 | `2384d6b`, `d6830cf`, `a588642`, `8f40418` | FASE 1 (cont.) | Full CRUD: appointments/employees/services/clients, inventory, reports/settings, missing sidebar routes — still `localStorage`-only. |
| 2025-11-25 | `8d6b555` | Auth migration | "Migrar autenticación de localStorage a Supabase y eliminar código demo" — strict TypeScript, security middleware, Zod validation, centralized error handling. Demo mode was removed here, then reintroduced days later (`c496a24`). |
| 2025-11-25 | `7f26d35` | Auth debug | RLS policy fixes, `useAuth.tsx` rename, "WIP: Debugging login issues." |
| 2025-11-28 | `0d6e354` | **FASE 1 – Paridad Crítica** | Public booking link (no login), Email/WhatsApp notification APIs, QR share page, Resend/Twilio scaffolding. |
| 2025-11-30 | `c496a24` | Security hardening | Rate limiting, input validation/XSS/SQLi detection, security headers (CSP/HSTS/X-Frame-Options), security logging, environment validation, 7 security docs; fixed `/login`→`/auth/login` routing; **re-introduced demo mode** with `localStorage` auth as a fallback ([ADR-002](04_DECISIONS.md)). |
| 2025-11-30 | `a02acc0` | Hybrid booking system | "Sistema de reservas híbrido" — login-based + no-registration booking + optional signup modal. |
| 2025-11-30 → 2026-04-24 | `84451d5`, `2fdf431` | Testing infrastructure | Vitest + Testing Library + Playwright setup; unit/integration/e2e tests, openspec specs, timezone bugfix. |
| Dec 2025 – Feb 2026 | `efd9619` … `5510d33` | Supabase wiring | Middleware enabled; dashboard/auth/routing connected to real Supabase; appointments, employees, services, inventory, clients progressively migrated off `localStorage`; multiple `fix:` commits for RLS bypass via service-role API routes. |
| 2026-05 | `1cea13b` | Visual rebrand | "Orno design system" — admin editorial redesign, logo, favicons, dark theme ([ADR-007](04_DECISIONS.md)). |
| 2026-05 | `7a09427` | Rebrand extension | Applied the same visual system to client/employee dashboards. |
| 2026-05-27 | `d6fb558` | Auth unification | "unificar sistema de auth" — corresponds to the `openspec/changes/archive/2026-05-27-unificar-auth` change ([ADR-006](04_DECISIONS.md), verified complete). |
| 2026-05-31 | `5579961` | Role experimentation | "sistema de roles de empleado (barbero/cajero/recepcionista/gerente)" — precursor to today's `manager`/`barber` role tech debt ([07_TECH_DEBT.md](07_TECH_DEBT.md)). |
| 2026-06-01 | `14bb85a` | **"M1-M8 feature pack"** | One commit shipping 8 unrelated features: M1 rescheduling, M2 commissions, M3 attendance/work-status, M4 password recovery, M5 ratings, M6 loyalty points, M7 POS, M8 low-rating alerts. **Not** a phased migration plan — see [ADR-014](04_DECISIONS.md) for why this must not be confused with the M0–M4 target scheme in [02_TARGET_ARCHITECTURE.md](02_TARGET_ARCHITECTURE.md). |
| 2026-06 | `2063afc` … `0530949` | v1.2 Items 9–19 | Explicitly numbered items: POS checkout/loyalty/tips (9-15), cache revalidation + pagination (16-17), notification queue (18), Resend/Twilio notification Edge Function (19). |
| 2026-06-29 | `f88af4a` | **FASE 1-3 audit** | "audit FASE 1-3 — booking wizard, auth security, UX y accesibilidad" (client-side). |
| 2026-06-29 | `69a8806`, `ab28d03`, `aa7dfbb`, `cadc018` | Admin audit | Admin panel audit, C5/D2 fixes, accessibility, demo-data consistency. |
| 2026-06-30 | (documentation revision, no code commit) | Security documentation correction | `SECURITY-REPORT.md`, `docs/GO-LIVE-PLAN.md`, `docs/EXECUTIVE-SUMMARY.md` revised against real code; corrected a prior self-contradiction (rate limiting/CSP/logging listed as both present and absent) and removed leaked plaintext secret fragments from the current revision (older versions remain in git history — see [07_TECH_DEBT.md](07_TECH_DEBT.md)). |
| 2026-07-01 | `46225e8` | QA | "smoke test de roles con playwright" → produced `qa-analysis.md`. |
| 2026-07-02 | `d4809b7` | Auth hardening | "harden demo login and middleware flow." |
| 2026-07-02 | `033fe16` | UI fix | Admin modal form contrast fix in dark theme. |
| 2026-07-03 | `fc80f3e` | **FASE 5 D2+D3** | "badges consistentes y banner demo en bloqueos" — employee schedule-block UI consistency. |
| 2026-07-03 | `5b3085a` | **FASE 5 D1** | "disponibilidad dinámica conectada con bloqueos" — booking availability wired to schedule blocks. Latest commit as of this writing. |
| 2026-07-05 | *(this session)* | **Project Brain created** | This documentation set (`project-brain/`) established as the permanent knowledge base; explicit separation of Current State vs. Target Architecture adopted after verifying that several architecture terms in the original request (BrandProvider, TenantProvider, AsyncPane, Application Shell, Scheduling Engine, Design Constitution, M0–M4) did not exist in the codebase or git history — see [ADR-014](04_DECISIONS.md). |
| 2026-07-05 | *(this session)* | **CRM Phase A** | Client-identity duplication (list/profile/POS) consolidated into `components/admin/clients/client-identity.tsx`; client profile CLV now includes `pos_sales`; added "Última visita"/"Próxima cita" stats. See [ADR-016](04_DECISIONS.md), [ADR-017](04_DECISIONS.md). |
| 2026-07-06 | *(this session)* | **Validation workflow adopted + pre-existing type bug fixed** | User established a standing rule: run `pnpm type-check`/`pnpm lint`/`pnpm build` after every implementation, stop and report on any error before touching unrelated code, never auto-commit. First run surfaced a pre-existing, unrelated type error in `hooks/useRequireAuth.ts:65` (from commit `eb1374a`, 2026-07-01) that had been silently breaking `type-check`/`build` for 5 days with no CI to catch it. Fixed with non-null assertions after explicit approval — see [07_TECH_DEBT.md](07_TECH_DEBT.md). |
| 2026-07-06 | *(this session)* | **CRM Phase B** | Additive schema authored (scripts 32-34: client profile fields, `memberships`, `client_attachments` + first Supabase Storage bucket in this codebase) and UI wired (`ClientPreferencesCard`, `ClientMembershipsCard`, `ClientAttachmentsCard`). `type-check`/`lint`/`build` all pass. Scripts not yet run against any live instance. See [ADR-018](04_DECISIONS.md). |
| 2026-07-06 | *(this session)* | **CRM Phase C — M5 feature-complete** | `ClientTimelineCard` (unified appointments/messages/gifts/POS feed), visit-frequency stat, and a deliberately static `ClientAIInsightsCard` placeholder. Closes the M5 CRM scope from the original request, with WhatsApp actions deferred to the Integrations module. `type-check`/`lint`/`build` all pass. See [ADR-019](04_DECISIONS.md). |
| 2026-07-06 | *(this session)* | **Sidebar logout-unreachable bug fixed** | User reported not seeing "Cerrar sesión" in `/admin` via a screenshot. The button existed in code; the bug was a missing `overflow-y` on each sidebar's `<nav>`, which let the fixed-height sidebar's footer render below short viewports with no way to scroll to it. Fixed identically in `AdminSidebar`, `EmployeeSidebar`, `ClientSidebar` (same copy-pasted layout, same bug). Verified visually with a throwaway Playwright script against the dev server at a 650px viewport — see [07_TECH_DEBT.md](07_TECH_DEBT.md). |
| 2026-07-06 | *(this session)* | **Logout label unified to "Cerrar sesión"** | User caught the employee mobile bottom nav saying "Salir" instead of "Cerrar sesión". Research found 2 more inconsistent copies ("Cerrar Sesión", capital S) in the generic `/dashboard` sidebar and the orphaned `/barber` page. All 3 unified; the deeper duplication (7 independent logout implementations, 2 of them dead code) logged as open debt rather than fixed in this pass — see [07_TECH_DEBT.md](07_TECH_DEBT.md). |
| 2026-07-06 | *(this session)* | **Billing/Integrations Phase A — stopped fabricating data** | Read both pages in full and confirmed 100% invented data (fake invoices, fake cards, fake "Stripe: Conectado", fake logs). Replaced with real (empty) `subscriptions`/`invoices` schema (`scripts/35-add-billing-schema.sql`) and a real Twilio/WhatsApp status check (`app/api/integrations/status`); the other 10 integrations relabeled "Próximamente" instead of fake-connected. No payment provider chosen or integrated — that decision remains open. `type-check`/`lint`/`build` all pass. See [ADR-020](04_DECISIONS.md). |
| 2026-07-06/07 | *(role cleanup)* | **Manager/barber role debt cleanup** | `febb8b2` removed `app/barber/*`, `app/api/alerts_backup/`, and updated 32 files so no application code branches on `role === 'manager'`/`'barber'` anymore; `manager` left as an inert Postgres enum + RLS artifact by design. `8ad62e8` documented it. |
| 2026-07-07 | *(this session)* | **`rotate-secrets` change closed — secret rotation confirmed executed** | User confirmed `TWILIO_AUTH_TOKEN`, `RESEND_API_KEY`, and `CRON_SECRET` were regenerated at the provider dashboards and updated in Vercel production + `.env.local` (both on their personal laptop and this machine), around 2026-06-30. `pnpm validate-env` run this session confirms all three pass. Not independently verifiable by the agent — external, manual provider/Vercel actions. Corrected a stale claim in `SECURITY-REPORT.md`/`docs/SECRET-ROTATION.md` that `scripts/validate-env.js` still had hardcoded plaintext secrets "today" — it had already been fixed in commit `1fa6145` (2026-06-30), before those docs were even written; confirmed clean by reading the file directly. Separately found (not part of this change): this machine's `.env.local` has placeholder Supabase keys and a malformed `TWILIO_ACCOUNT_SID` — logged as its own low-severity item, see [07_TECH_DEBT.md](07_TECH_DEBT.md). `openspec/changes/rotate-secrets` archived to `openspec/changes/archive/2026-07-07-rotate-secrets`. |
| 2026-07-06 | *(this session)* | **`manager`/`barber` role debt cleaned up (M0 priority #2)** | Removed both roles from ~20 files of live application code (`middleware.ts`, auth hooks, 8 admin-page guards, 6+ API routes, dispatcher, sidebar, login redirect map) — `barber` was never a real Postgres enum value (demo-only), `manager` remains a real enum value + RLS policies with zero live callers left. Deleted the orphaned `/barber` route and 3 dead duplicate `alerts_backup` routes. Fixed `app/admin/employees/page.tsx` to check `specialty` instead of the nonexistent `role === "barber"`, and a pre-existing bug in unused `lib/constants.ts` (`DASHBOARD_BY_ROLE.employee` pointed at `/barber`). Updated `e2e/employee.e2e.spec.ts` to stop referencing the deleted route — not verified by running it (no Supabase credentials this session). `type-check`/`lint`/`build` all pass. See [ADR-003](04_DECISIONS.md). |
| 2026-07-09 | `3fb0dc7`, `14996d6`, `9ffcd4b`, `1f68cc9`, `22bda7c` | **CRM framework migration (CRM-0 → CRM-4)** | Whole `admin/clients` module migrated to the ORNO framework, presentation-only (data layer verified byte-identical against the diff). ActionMenu evolved in the framework first (ADR-023 rule) and adopted by Agenda, then the client list (CRM-1), then the full profile (CRM-2A/2B: AsyncPane + shaped skeletons, StatusBadge, 7 StatCards, notify(), Textarea; module-wide raw-palette grep = 0). Includes a hotfix for a pre-existing demo-mode bug in `POST /api/loyalty` (UUID schema validated before the `isDemoMode()` branch, so demo ids `'c1'…` always got 422). Validated per milestone with type-check/lint/vitest plus an 11-step Playwright E2E smoke in forced demo mode (11/11 PASS, 0 console errors). CRM-4 closed the module as a documentation-only commit — see [ADR-024](04_DECISIONS.md). |
| 2026-07-09 | `354681c` | **FW-1 — NativeSelect + ClientAvatar.imageUrl** | `NativeSelect` evolved as a framework primitive (wraps the native `<select>` with ORNO tokens matching `Input`); `ClientAvatar` received an additive `imageUrl` prop to avoid bifurcating a component already adopted in CRM and POS. Both landed in the framework before any module consumed them — per the ADR-023 rule. |
| 2026-07-09 | `ba197f8` | **EMP-1 — Employees + Appointments modals adopt NativeSelect** | `app/admin/employees/page.tsx` and `components/admin/employees/employee-modal.tsx` migrated to the ORNO framework (presentation-only; data layer byte-identical). Key changes: `NativeSelect` replaces the local `SELECT_CLS` constant; `ClientAvatar.imageUrl` replaces the inline conditional `<img>`; `TempPasswordAlert` introduces a persistent `Alert`-based panel for temporary credentials (correct pattern for operational information the admin must copy — toast was wrong here). The Agenda's create/edit modal (`components/admin/appointments/appointment-modal.tsx`) also adopts `NativeSelect` as its first colateral consumer, serving as a regression test for the framework primitive. DEMO_EMPLOYEES desynchronization found and explicitly excluded from scope — see [ADR-025](04_DECISIONS.md) and [07_TECH_DEBT.md](07_TECH_DEBT.md). |

---

**Related**: [01_CURRENT_STATE.md](01_CURRENT_STATE.md) · [04_DECISIONS.md](04_DECISIONS.md) · [05_ROADMAP.md](05_ROADMAP.md)
